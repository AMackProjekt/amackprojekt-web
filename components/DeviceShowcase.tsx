"use client";
import { useState } from "react";
const roles=[{name:'Participant',title:'Your next step.',items:['Goals & profile','Messages','Access requests']},{name:'Staff',title:'The work, connected.',items:['Caseload & notes','Documents','Scheduling']},{name:'Admin',title:'See the whole picture.',items:['Oversight & audit','Executive operations','HR workflows']}];
export function DeviceShowcase(){const [active,setActive]=useState(1);return <div className="device-showcase"><div className="device-visual" onPointerMove={e=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches||document.documentElement.dataset.ampMotion==='paused')return;const r=e.currentTarget.getBoundingClientRect();e.currentTarget.style.setProperty('--device-x',((e.clientX-r.left)/r.width-.5)*9+'deg');e.currentTarget.style.setProperty('--device-y',((e.clientY-r.top)/r.height-.5)*-6+'deg');}} onPointerLeave={e=>{e.currentTarget.style.setProperty('--device-x','0deg');e.currentTarget.style.setProperty('--device-y','0deg');}}><img className="device-scene" src="/brand/amp-device-scene.webp" alt="Futuristic concept visualization of a floating laptop and connected interface layers" width="1600" height="900" loading="lazy"/><div className="device-brand"><img src="/brand/projekt-enterprise-icon.svg" alt="CaseFlow Operations" width="36" height="36"/><div>CaseFlow<span>OPERATIONS</span></div></div><div className="device-pane" key={active}><p>PROJEKT ENTERPRISE / {roles[active].name.toUpperCase()}</p><h3>{roles[active].title}</h3><ul>{roles[active].items.map((item,i)=><li key={item}><span>0{i+1}</span>{item}</li>)}</ul></div><div className="device-tag"><span aria-hidden="true"/>Participant. Staff. Admin.<small>One connected platform.</small></div></div><div className="device-role-switch" aria-label="Explore enterprise roles">{roles.map((role,i)=><button key={role.name} aria-pressed={active===i} onClick={()=>setActive(i)}>{role.name}<span aria-hidden="true">↗</span></button>)}</div><p className="device-caption">Product overview · Concept visualization</p></div>}
